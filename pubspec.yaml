name: easy_ozow
description: An easy flutter package to implement Ozow payment gateway into your flutter app in two steps.
version: 0.0.8
homepage: https://github.com/wdtheprovider/easy_ozow

environment:
  sdk: '>=3.0.6 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^4.10.0
  webview_flutter_wkwebview: ^3.17.0
  webview_flutter_android: ^4.3.1
  path_provider: ^2.1.5
  crypto: ^3.0.6
  http: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  

flutter:

platforms:
  android:
  ios:
  macos: