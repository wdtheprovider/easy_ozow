name: flutter_ozow
description: A flutter package to work with ozow payments
version: 0.0.1
homepage:

environment:
  sdk: '>=3.0.6 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^4.2.2
  webview_flutter_wkwebview: ^3.7.3
  webview_flutter_android: ^3.9.3
  path_provider: ^2.1.0
  crypto: ^3.0.3
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  

flutter:
